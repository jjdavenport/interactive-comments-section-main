import icon from "./assets/icon-reply.svg";

const Reply = () => {
  return (
    <>
      <button>
        <img src={icon} alt={icon} />
        Reply
      </button>
    </>
  );
};

export default Reply;
