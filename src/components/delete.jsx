import icon from "./assets/icon-delete.svg";

const Delete = () => {
  return (
    <>
      <button>
        <img src={icon} alt={icon} />
        Delete
      </button>
    </>
  );
};

export default Delete;
